import type { PropsWithChildren } from "react";

export function ParallaxMedia({ children }: PropsWithChildren) {
  // TODO: Add parallax only after motion direction and media assets are approved.
  return <div>{children}</div>;
}

