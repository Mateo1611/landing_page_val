# Future 3D Layer

This folder is intentionally empty for now. Do not install or implement `three`,
`@react-three/fiber`, or `@react-three/drei` until a later phase explicitly
requires 3D direction, assets, and performance constraints.

