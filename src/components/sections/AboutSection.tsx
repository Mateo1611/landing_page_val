import { SectionLabel } from "@/components/ui/SectionLabel";

export function AboutSection() {
  return (
    <section className="mx-auto max-w-6xl px-6 py-[var(--section-padding)]" id="about">
      <SectionLabel>About</SectionLabel>
      <p className="max-w-2xl text-[var(--color-muted)]">
        Bloque temporal para historia de marca. Debe reemplazarse con contenido final aprobado.
      </p>
    </section>
  );
}

